export type GolferDTO = {
    username: string,
    firstName: string | null,
    lastName: string | null,
    handicap: number | null,
    homeGolfCourseId: number | null,
    profilePicture: any | null
}